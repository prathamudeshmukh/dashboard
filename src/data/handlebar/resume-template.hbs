<!DOCTYPE html>
<html>
<head>
<style>
  body {
    font-family: 'Segoe UI', Arial, sans-serif;
    color: #333;
    line-height: 1.5;
    margin: 0;
    padding: 20px;
    background-color: #f9f9f9;
  }
  .resume-container {
    max-width: 800px;
    margin: 0 auto;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    padding: 40px;
  }
  .header {
    text-align: center;
    margin-bottom: 30px;
  }
  .name {
    font-size: 28px;
    font-weight: bold;
    margin-bottom: 5px;
    color: #2c5282;
  }
  .title {
    font-size: 18px;
    color: #4a5568;
    margin-bottom: 15px;
  }
  .contact-info {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 15px;
    font-size: 14px;
  }
  .contact-item {
    display: flex;
    align-items: center;
    gap: 5px;
  }
  .section {
    margin-bottom: 25px;
  }
  .section-title {
    font-size: 18px;
    font-weight: bold;
    margin-bottom: 15px;
    padding-bottom: 5px;
    border-bottom: 2px solid #2c5282;
    color: #2c5282;
  }
  .summary {
    margin-bottom: 25px;
  }
  .experience-item, .education-item, .project-item {
    margin-bottom: 20px;
  }
  .item-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 5px;
  }
  .item-title {
    font-weight: bold;
    font-size: 16px;
  }
  .item-subtitle {
    font-style: italic;
    color: #4a5568;
  }
  .item-date {
    color: #718096;
    font-size: 14px;
  }
  .item-description {
    margin-top: 10px;
  }
  .skills-list {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 10px;
  }
  .skill-item {
    background-color: #ebf4ff;
    color: #2c5282;
    padding: 5px 10px;
    border-radius: 15px;
    font-size: 14px;
  }
  .bullet-list {
    margin-top: 10px;
    margin-left: 20px;
    padding-left: 0;
  }
  .bullet-item {
    margin-bottom: 5px;
    list-style-type: disc;
  }
  .references {
    font-style: italic;
    text-align: center;
    margin-top: 30px;
    color: #718096;
  }
</style>
</head>
<body>
<div class="resume-container">
  <div class="header">
    <div class="name">{{person.name}}</div>
    <div class="title">{{person.title}}</div>
    <div class="contact-info">
      <div class="contact-item">{{person.email}}</div>
      <div class="contact-item">{{person.phone}}</div>
      <div class="contact-item">{{person.location}}</div>
      <div class="contact-item">{{person.linkedin}}</div>
      <div class="contact-item">{{person.website}}</div>
    </div>
  </div>

  <div class="section summary">
    <div class="section-title">Professional Summary</div>
    <p>{{summary}}</p>
  </div>

  <div class="section">
    <div class="section-title">Experience</div>
    {{#each experience}}
    <div class="experience-item">
      <div class="item-header">
        <div>
          <div class="item-title">{{title}}</div>
          <div class="item-subtitle">{{company}}</div>
        </div>
        <div class="item-date">{{dateRange}}</div>
      </div>
      <div class="item-description">
        <p>{{description}}</p>
        <ul class="bullet-list">{{#each achievements}}<li>{{this}}</li>{{/each}}</ul>
      </div>
    </div>
    {{/each}}
  </div>

  <div class="section">
    <div class="section-title">Education</div>
    {{#each education}}
    <div class="education-item">
      <div class="item-header">
        <div>
          <div class="item-title">{{degree}}</div>
          <div class="item-subtitle">{{institution}}</div>
        </div>
        <div class="item-date">{{dateRange}}</div>
      </div>
      <div class="item-description"><p>{{details}}</p></div>
    </div>
    {{/each}}
  </div>

  <div class="section">
    <div class="section-title">Skills</div>
    <div class="skills-list">{{#each skills}}<div class="skill-item">{{this}}</div>{{/each}}</div>
  </div>

  <div class="section">
    <div class="section-title">Projects</div>
    {{#each projects}}
    <div class="project-item">
      <div class="item-header">
        <div class="item-title">{{title}}</div>
        <div class="item-date">{{date}}</div>
      </div>
      <div class="item-description"><p>{{description}}</p></div>
      <div class="skills-list">{{#each technologies}}<div class="skill-item">{{this}}</div>{{/each}}</div>
    </div>
    {{/each}}
  </div>

  <div class="section">
    <div class="section-title">Certifications</div>
    <ul class="bullet-list">
      {{#each certifications}}
      <li>{{name}} ({{issuer}}) • {{date}}</li>
      {{/each}}
    </ul>
  </div>

  <div class="section">
    <div class="section-title">Languages</div>
    <ul class="bullet-list">{{#each languages}}<li>{{language}}: {{proficiency}}</li>{{/each}}</ul>
  </div>

  <div class="references">{{references}}</div>
</div>
</body>

</html>
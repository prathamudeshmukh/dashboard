export type FeatureContent = {
  id: string;
  title: string;
  content: string;
  points?: string[];
};

export type FeatureAccordionItemProps = {
  value: string;
  title: string;
  description: string;
  points?: string[];
};
